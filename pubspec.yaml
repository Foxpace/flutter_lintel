name: flutter_arch_guard
description: Analyzer plugin that enforces cohesive Flutter application architecture.
version: 0.1.0

topics:
  - analyzer
  - architecture
  - flutter
  - lints
  - static-analysis

environment:
  sdk: ^3.12.2

dependencies:
  analysis_server_plugin: ^0.3.20
  analyzer: ^14.1.0
  yaml: ^3.1.3

dev_dependencies:
  analyzer_testing: ^0.3.4
  lints: ^6.1.0
  test: ^1.31.2
  test_reflective_loader: ^0.4.0
